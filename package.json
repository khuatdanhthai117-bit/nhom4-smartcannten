{
  "name": "smart-canteen",
  "version": "1.0.0",
  "private": true,
  "description": "Hệ thống đặt món và vận hành căn tin (bản demo có cấu trúc server).",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/store.js && node --check src/auth.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
